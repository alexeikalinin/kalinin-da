# API / Application Layer

Статус: Draft v0.1 — на ревью
Назначение: описать "входную дверь" платформы — то, через что вы (и в будущем White-label пользователи) реально взаимодействуете с системой: создание Project, пауза на подтверждение, просмотр прогресса, получение результата. Этот документ был пропущен на первом проходе Architecture Overview (он был единственной "комнатой" на диаграмме C4 без отдельного описания) — добавлен по итогам независимого технического ревью пакета документации.

Простыми словами: если все остальные документы раздела 03 описывают, что происходит "за кулисами", то этот — про сцену, на которую выходит пользователь: как именно он говорит системе "начни" и как система говорит ему "готово".

Использует термины из [[Glossary]]. Реализует [[Functional-Requirements]] (FR-1…FR-6, FR-8) и [[Non-Functional-Requirements]] (NFR-8, NFR-9, NFR-14). Опирается на [[Workflow-Engine]] (раздел 3 — Strategy-gate), [[Architecture-Overview]] (ADR-0001).

---

## Purpose

Закрыть разрыв, найденный независимым ревью: до этого документа примерно половина Functional Requirements (всё, что касается прямого взаимодействия пользователя) не имела архитектурного описания, кроме одной подписи на диаграмме.

## Scope

Документ описывает: аутентификацию (в текущем, сознательно упрощённом виде), контракт создания Project, контракт паузы/возобновления на Strategy-gate, механизм просмотра прогресса, механизм уведомления о завершении (сейчас — заглушка), точку внедрения брендирования для White Label. Не описывает: конкретную реализацию UI (Этап 2), сам протокол Telegram-бота (появится отдельно, когда будет токен — см. раздел 4).

---

## 1. Аутентификация (текущее сознательное упрощение)

**Решение (2026-08-04):** на первой итерации системой пользуетесь только вы — полноценная аутентификация (регистрация, пароли, множество пользователей) не реализуется. Технически это означает: один тенант (Owner), доступ к системе — без публичного экрана входа, до момента, когда встанет задача показывать платформу другим пользователям.

**Важно для архитектуры:** это упрощение UI/входа, а не упрощение модели данных — `tenant_id` и row-level security (Database, Security) реализуются полноценно с первой итерации независимо от того, что реального "экрана логина" пока нет. Это значит, что включение полноценной аутентификации в будущем (Backlog) — это добавление слоя входа поверх уже готовой изоляции, а не переделка хранения данных.

## 2. Создание Project (FR-1, FR-2, FR-2a)

Точка входа принимает: URL сайта, описание бизнеса, продукт, маркетинговую задачу, выбранный Approval Level (по умолчанию Strategy-gate), выбранный Execution Tier (Fast/Standard). По приёму — создаётся PROJECT (Database) и запускается PM Agent (Workflow Engine, раздел 1).

## 3. Контракт паузы/возобновления на Strategy-gate (FR-2, детализация Workflow Engine раздел 3)

Раньше это было отмечено как открытый вопрос ("что именно показывается на паузе"). Закрываю здесь минимально необходимым для Этапа 2 контрактом:

- Система переводит Project в статус "ожидает подтверждения плана" и делает план (список ролей и порядок, построенный PM Agent) доступным для просмотра.
- Пользователь выполняет одно из двух действий: **подтвердить** (Project переходит в исполнение) или **отклонить с комментарием** (PM Agent строит план заново с учётом комментария).
- Технически это обычный запрос/ответ (нет необходимости в realtime-канале для этого шага — соответствует уже принятому решению NFR-8, "достаточно periodic pull").

## 4. Просмотр прогресса и уведомление о завершении (FR-3, FR-4, NFR-8, NFR-9)

**Прогресс (FR-3):** реализуется как обычный запрос состояния Project (periodic pull, NFR-8) — при каждом открытии статуса возвращается текущее состояние графа Task (Events/Logging-Audit, раздел "прогресс").

**Уведомление о завершении (FR-4, NFR-9 — гарантированная доставка):** решение (2026-08-04) — канал доставки: **Telegram-бот**, который будет подключён отдельно, когда вы предоставите токен бота. **На первой итерации (пока бота нет) этот функционал не реализуется** — гарантия доставки (NFR-9) на это время ослаблена до "статус доступен при просмотре прогресса", без проактивного уведомления. Это сознательное временное отступление от NFR-9, а не отмена требования — как только Telegram-бот подключён, NFR-9 реализуется полноценно через него.

## 4a. Получение результата и запрос доработки (FR-5, FR-6)

**Получение результата (FR-6):** по завершении Project (статус Project Completed, Events) точка входа отдаёт Project Output — гибкий набор материалов, собранный Report Generator Agent из результатов только тех ролей, что были задействованы (Functional Requirements, FR-6, решение от 2026-08-04). Это отдельный, самостоятельный запрос, не совпадающий с запросом прогресса (раздел 4) — прогресс отдаёт статус, эта точка отдаёт готовые материалы.

**Запрос доработки (FR-5):** доступен только для Project в статусе Completed. Пользователь отправляет комментарий/уточнение — система создаёт новую Task в рамках **того же** Project (не новый Project, переиспользуется Project Memory — Product Specification, раздел 2.1), направленную тому же PM Agent для перепланирования затронутой части Workflow. Approval Level и Execution Tier исходного Project наследуются, если не указано иное.

## 5. Точка внедрения брендирования (FR-8, White Label)

Хотя White Label не активен на первой итерации (референсный сценарий — Owner), точка для брендирования архитектурно закладывается уже сейчас, чтобы не потребовалось переделывать API-слой позже: любой ответ, содержащий Project Output или уведомление, проходит через слой оформления, который для тенанта типа "White-label клиент" подставляет его бренд, а для Owner — не подставляет ничего лишнего. Реализация самого оформления — Этап 2, вне документации.

---

## Dependencies

- Зависит от: [[Functional-Requirements]], [[Non-Functional-Requirements]], [[Workflow-Engine]], [[Architecture-Overview]].
- От него зависят: `Deployment` (условия готовности), `Repository Structure` (папка `apps/web`).

## Open Questions

1. **Полноценная аутентификация для нескольких пользователей.** Сознательно отложено (раздел 1) — в Backlog.
2. **Протокол интеграции с Telegram-ботом.** Появится как отдельная задача, когда будет токен — не архитектурное решение уровня Этапа 1, а конкретная интеграционная работа Этапа 2.

## Decision Log

| Дата | Решение | Автор |
|---|---|---|
| 2026-08-04 | Создан по итогам независимого технического ревью (Finding 1 — отсутствовал документ для API/Application Layer). Аутентификация упрощена до одного пользователя на первой итерации без изменения модели изоляции данных; уведомление через Telegram-бот отложено до предоставления токена; контракт Strategy-gate закрыт минимально необходимым описанием; точка брендирования заложена заранее | Draft, ожидает утверждения |
| 2026-08-04 | По итогам повторного ревью: добавлен раздел 4a (FR-5 запрос доработки, FR-6 получение результата — ранее не были явно закрыты, хотя заявлялись в Purpose) | Пользователь (по результатам повторного ревью) |
| 2026-08-12 | Реализовано в коде: `apps/web/app/api/projects/*` (Next.js Route Handlers) + `apps/web/lib/orchestrator.ts`, реально связывающий CEO/PM/все 13 ролей. Проверено настоящими HTTP-запросами через `next dev`: создание Project → пауза Strategy-gate → подтверждение → полное исполнение графа → получение Project Output; отдельно проверены отклонение плана (пересборка) и запрос доработки. Известное упрощение: исполнение графа синхронно внутри запроса на approve (нет фонового воркера); rework реализован как перезапуск только Report Generator, а не выборочная передоработка через PM (см. README `apps/web` и комментарий в `orchestrator.ts`) | Пользователь |
| 2026-08-12 | Подключён настоящий Anthropic API-ключ (`apps/web/.env.local`, не в git): все 13 ролей теперь вызывают реальную модель Claude через `apps/web/lib/anthropic.ts` (structured JSON вывод через tool-forcing) и `apps/web/lib/real-models.ts`, вместо заглушек. Переключение автоматическое: `USE_REAL_MODELS` в `orchestrator.ts` включается, если ключ задан, иначе система по-прежнему работает на заглушках (не падает без ключа, например в CI). Проверено настоящим сквозным прогоном (создание Project для кафе → одобрение → все 11 задач графа выполнены реальными вызовами модели → получен Project Output). Известное ограничение: инструменты (доступ к сайту, рекламные кабинеты, аналитика) остаются заглушками — поздние роли получают мало реального контекста о клиенте, из-за чего часть результата на поздних этапах общая, а не специфичная; это ожидаемо устранится на этапе подключения настоящих инструментов, не моделей | Пользователь |
| 2026-08-12 | Подключены первые два реальных инструмента (Tool Integration §1): `site-reader` — настоящий HTTP-скрейпинг сайта клиента, без ключей (`apps/web/lib/tools/site-reader.ts`); `web-search` — Perplexity API, модель `sonar` (`apps/web/lib/tools/web-search.ts`, ключ в `apps/web/.env.local`, не в git). Оба подключены через `apps/web/lib/real-tools.ts` (реализация `ToolInvoker`), используются только Research Agent'ом — единственной ролью, которой они назначены (Tool Integration §2). `real-models.ts`'s `realResearch` исправлен: раньше реальные `siteContent`/`searchResults` от инструментов доходили до модели и отбрасывались (`void siteContent`) — теперь они реально попадают в промпт как `clientFacts`. Проверено сквозным прогоном на реальном сайте (wikipedia.org): находки Research Agent содержат точные факты с реальной страницы (число статей по языкам и т.п.), которые модель не могла бы выдумать — подтверждает, что скрейпинг и поиск реально используются, а не игнорируются. Остальные инструменты (seo-service, google-ads, vk-ads, google-analytics, design-tool, deployment-tool) остаются заглушками — требуют либо платных API, либо OAuth-подключения к реальным рекламным кабинетам, отложено до отдельного решения | Пользователь |
| 2026-08-13 | Подключён реальный Google Ads API (v25 REST) для PPC Agent (`apps/web/lib/tools/google-ads.ts`), используя реальный аккаунт пользователя: управляющий `855-960-6792` (login-customer-id) и клиентский `347-605-5035` (customer-id). OAuth-приложение и Developer Token созданы в Google Cloud/Google Ads, refresh token получен через одноразовую авторизацию (см. `apps/web/.env.local`, не в git). Реализовано чтение существующих кампаний (GAQL search) и создание кампании — при этом каждая созданная кампания сознательно оставляется в статусе PAUSED (реальный расход рекламного бюджета не включается автоматически ни при каких условиях на этом этапе); создание идемпотентно по имени кампании, чтобы повторные прогоны не плодили дубликаты. Дневной бюджет на канал вычисляется как доля (`budgetShare` из результата модели) от заглушечного дефолтного общего бюджета — известное упрощение: реальный бюджет проекта пока не передаётся в этот вызов инструмента, это будет исправлено, когда PPC-роль получит доступ к реальным данным о бюджете проекта. Проверено настоящим сквозным прогоном: PPC Agent реально создал и затем удалил (после проверки) настоящую PAUSED-кампанию в аккаунте пользователя. `vk-ads` остаётся заглушкой | Пользователь |
| 2026-08-13 | Подключены реальные Google Analytics (GA4 Admin API) и Google Tag Manager (API v2) для Analytics Agent (`apps/web/lib/tools/google-analytics.ts`, `google-tag-manager.ts`, общий OAuth-хелпер вынесен в `google-oauth.ts`, один refresh token теперь покрывает все три интеграции — Ads/Analytics/Tag Manager). Для каждого Project провиженится (идемпотентно по имени) отдельный GA4 Property + Web Data Stream, отдельный GTM Container с опубликованным тегом GA4 Configuration (триггер "All Pages"), событие помечается как конверсия (`keyEvents`) и GA4-ресурс линкуется к Google Ads (`googleAdsLinks`) — весь путь от клика до появления конверсии в Google Ads выстроен программно. Известное архитектурное решение (сознательное отступление от "Analytics Agent только анализирует, не настраивает" — Agent Framework §responsibility): в текущей реализации создание инфраструктуры отслеживания встроено в сам вызов инструмента `google-analytics`, а не выделено в отдельную роль/этап — задокументировано здесь как явный компромисс, не скрытая ошибка. Технически подтверждено ограничение Google Tag Manager API: **создание top-level Account** через API невозможно (только Container и всё, что ниже) — поэтому под каждое агентство заводится один родительский GTM Account и один родительский GA Account вручную (см. `GOOGLE_GTM_ACCOUNT_ID`/`GOOGLE_GA_ACCOUNT_ID` в `.env.local`), а Container/Property на каждого клиента создаются автоматически внутри них. **Известное ограничение на масштаб** (обсуждено с пользователем 2026-08-13): сейчас родительские GTM/GA-аккаунты и Google Ads customer_id — это глобальные переменные окружения ("один клиент по умолчанию", тот же паттерн, что и остальной Stage 1); для реальной работы с разными клиентами эти ID должны стать частью данных конкретного Project (как уже `siteUrl`), а не глобальной конфигурацией — запланировано отдельным шагом. Проверено настоящим сквозным прогоном: реальный GA4 Property + GTM Container с опубликованной GA4-конфигурацией созданы и подтверждены через API для тестового Project, затем тестовые ресурсы удалены | Пользователь |
