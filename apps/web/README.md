# @ama/web

Implements: `docs/03-architecture/api-application-layer.md` (API/Application Layer — user-facing entry point).

Package skeleton (Stage 2, step 1) — no business logic yet. Auth, Project creation, Strategy-gate contract, progress polling, and rework/output endpoints are all pending implementation per the referenced document.
